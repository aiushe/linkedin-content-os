# Synthetic development corpus

This directory is deliberately fictional. It exists so the harness, gates, and
eval suite can run without reading or changing `private/`. Do not replace these
records with personal material; Phase K uses the ignored `private/` corpus.
